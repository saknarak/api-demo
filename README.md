# DEMO NODE API

## PACKAGE UPDATE
```sh
npm i cors express express-fileupload knex mysql

npm i -D babel-core babel-eslint babel-preset-env cross-env eslint eslint-config-standard eslint-plugin-import eslint-plugin-jest eslint-plugin-node eslint-plugin-promise eslint-plugin-standard jest jest-environment-node
```